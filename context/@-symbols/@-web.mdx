---
title: "@网络"
---

### `@网络`

<Frame>
<img src="/images/context/@web.png" />
</Frame>

通过 `@网络`，Cursor 会根据您提供的查询和上下文构建搜索查询，并搜索网络以
找到相关信息作为额外的上下文。这对于找到最新的信息特别有用。

### 始终开启

您可以通过在 `Cursor 设置` > `功能` > `聊天` 下开启"始终搜索网络"，让 Cursor 在每次查询时都搜索网络。这相当于
在每次查询时都使用 `@网络`。

